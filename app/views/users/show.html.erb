<h1><%= @user.name %></h1>
<p><%= @user.email %></p>
<h1><%= "#{@user.name}'s Events" %></h1>
<% @user.created_events.each do |event| %>
    <%= render 'layouts/event', :event => event  %>
  <% end %>
</div>
</div>
